export interface IListItem {
    item: {
        id: number;
        name: string;
        isFavorite: boolean;
    };
}

export interface IListItemData {
    item: {
        name: string;
        isFavorite: boolean;
    };
}
